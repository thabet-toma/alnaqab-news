# Logs and caches
*.log
.DS_Store
Thumbs.db

# Environment and sensitive local config
.env
.env.local

# إعدادات كل سيرفر على حدة — تحتوي كلمات السر، لا تُرفع أبداً
includes/config.php

# Uploads content (optional - preserve directory structure)
uploads/articles/*
!uploads/articles/.gitkeep
uploads/ads/*
!uploads/ads/.gitkeep

# Local server config and credentials
includes/config.local.php
.db-credentials
